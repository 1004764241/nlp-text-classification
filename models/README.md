# 模型目录

存放训练产出的模型文件，例如：

```
models/
└── exp001_tfidf_svm/
    ├── model.joblib
    ├── vectorizer.joblib
    └── meta.json
```

该目录内容已在 `.gitignore` 中排除，不会上传到 GitHub。
请在实验报告或 `results/` 中记录模型路径与超参数。
